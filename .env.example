# BSC Network Configuration
BSC_RPC_URL_PRIMARY=https://bsc-dataseed1.binance.org/
BSC_RPC_URL_FALLBACK=https://bsc-dataseed2.binance.org/

# API Keys (required)
ONEINCH_API_KEY=your_oneinch_api_key_here
MORALIS_API_KEY=your_moralis_api_key_here

# Ramp direct link (preferred). Put full URL including hostApiKey; app will append userAddress, fiatCurrency, fiatValue.
# Example for BSC USDT, amount 100 USD (amount/currency can be overridden in app):
# RAMP_BUY_URL=https://app.ramp.network/?hostApiKey=YOUR_API_KEY&swapAsset=BSC_USDT
RAMP_BUY_URL=

# Transak direct link (fallback). Put full URL including apiKey; app will append walletAddress, network, fiatCurrency, fiatAmount.
# Example for BSC USDT, amount 100 USD:
# TRANSAK_BUY_URL=https://global.transak.com?apiKey=YOUR_API_KEY&network=bsc&cryptoCurrencyCode=USDT
TRANSAK_BUY_URL=

# Optional Proxy URLs (recommended for production to keep API keys on server)
# Point these to your backend (Render) service so the mobile app never sends API keys directly.
# Example: https://your-backend.example.com/api/moralis and /api/oneinch
MORALIS_PROXY_URL=https://your-backend.example.com/api/moralis
ONEINCH_PROXY_URL=https://your-backend.example.com/api/oneinch

# Environment
APP_ENV=dev
BACKEND_BASE_URL=

